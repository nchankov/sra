# Email channel for SRA

Sending message using mail script. This requires the following libs to be installed:

```
sudo apt install mailutils
```

Create .env file and put an email address into $EMAIL variable